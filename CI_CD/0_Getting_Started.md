# Continuous Integration / Continuous Deployment

## CI

- Code that changes are frequently combined into a shared repository. This process includes automated checks for new code to see if it works with old code.
- Create a pipeline after commit, usually `build -> test`.

